{
  "name": "phileasdg-blog",
  "version": "1.0.0",
  "description": "Static blog generation tools",
  "main": "scripts/build-posts.js",
  "type": "module",
  "scripts": {
    "build": "BUILD_MODE=release node scripts/build-posts.js --release",
    "build:dev": "BUILD_MODE=dev node scripts/build-posts.js --dev",
    "build:release": "BUILD_MODE=release node scripts/build-posts.js --release",
    "dev": "BUILD_MODE=dev node scripts/server.js",
    "validate": "node scripts/validate.js",
    "test": "node scripts/validate.js"
  },
  "dependencies": {
    "sharp": "^0.35.3"
  }
}
